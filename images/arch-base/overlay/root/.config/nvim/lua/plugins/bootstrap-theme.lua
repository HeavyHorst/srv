-- Managed by srv's Arch base image overlay. Remove this file if you want to
-- manage the default LazyVim colorscheme manually inside the guest.
return {
  {
    "LazyVim/LazyVim",
    opts = {
      colorscheme = "bootstrap_bmw_heritage_amber_dark",
    },
  },
}
